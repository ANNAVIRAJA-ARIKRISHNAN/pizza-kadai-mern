{
  "name": "pizza-kadai",
  "version": "1.0.0",
  "description": "Pizza kadai site",
  "scripts": {
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm install --prefix backend && npm run build --prefix client",
    "start": "npm start --prefix backend",
    "dev": "npm run build --prefix client && npm run dev --prefix backend"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
